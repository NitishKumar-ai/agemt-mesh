# Docker Compose stack for Playwright UI integration tests.
#
# Uses the locally-built agentmesh:server image with Postgres as the backing
# store (no Elasticsearch required — Postgres full-text search is sufficient
# for the UI test workload).
#
# Build the server image once before running integration tests:
#
#   docker build -t agentmesh:server -f docker/server/Dockerfile \
#     --build-arg PREBUILT=false .
#
# Then start this stack (from the repo root):
#
#   docker compose -f docker/docker-compose-ui-e2e.yaml up -d
#
# The Playwright global-setup script does this automatically when tests start.
# Container names are distinct from other compose stacks to allow both to
# run side-by-side without port or name conflicts.

services:
  agentmesh-server:
    image: agentmesh:server
    build:
      context: ../
      dockerfile: docker/server/Dockerfile
    container_name: agentmesh-server-ui-e2e
    environment:
      - CONFIG_PROP=config-postgres.properties
      - agentmesh.app.ownerEmailMandatory=false
    networks:
      - internal
    ports:
      - "8000:8080"
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/health"]
      interval: 10s
      timeout: 10s
      retries: 24  # up to 4 minutes for a cold JVM start
    links:
      - agentmesh-postgres:postgresdb
    depends_on:
      agentmesh-postgres:
        condition: service_healthy
    logging:
      driver: "json-file"
      options:
        max-size: "10k"
        max-file: "3"

  agentmesh-postgres:
    image: postgres:16
    container_name: agentmesh-postgres-ui-e2e
    environment:
      - POSTGRES_USER=agentmesh
      - POSTGRES_PASSWORD=agentmesh
    networks:
      - internal
    healthcheck:
      test: timeout 5 bash -c 'cat < /dev/null > /dev/tcp/localhost/5432'
      interval: 5s
      timeout: 5s
      retries: 12
    logging:
      driver: "json-file"
      options:
        max-size: "1k"
        max-file: "3"

networks:
  internal:
